#include<stdio.h>
int main()
{
	int a,b,t;
	printf("enter the the distance (in km)\n");
	scanf("%d",&a);
	printf("enter the the distance (in m) is %d\n",a*1000);
		printf("enter the the distance (in cm) %d\n",a*100000);
    printf("enter the the distance (in mm) %d\n",a*1000000);
    
    return 0;
}
				
